#include "main.h"

/**
 * _islower(int c) tells if a character is lower
 *
 * Return: Always 0.
 */
int  _islower(int c)
{
	int a;
	char b;

	for (b = 'a'; b <= 'z'; b++)
	{
		if (c == b)
		{
			_putchar(
		}
	}
	_putchar('\n');
}
