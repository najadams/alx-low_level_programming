#include <stdio.h>

/**
 * mul- return the product of two numbers
 * @b : stores one of the variable
 * @a : this is the variable to store input
 * Return: the value of the product
 */

int mul(int a, int b)
{
	int ans;

	ans = a * b;
	return (ans);
}
